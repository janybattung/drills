function isSameLength(word1, word2) {
    if (word1.length === word2.length) {
      return true;
    } else {
      return false;
    }
   }
   let y = isSameLength ('bite', 'bet');
   console.log (y);